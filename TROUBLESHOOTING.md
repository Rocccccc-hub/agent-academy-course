# 🔧 课程系统问题排查指南

## 当前问题

用户报告：
1. ✅ 视频只有 2 分钟（已更新duration为 2:00）
2. ❓ 课程内容显示空白

## 已完成的修复

### 1. 视频文件
- ✅ 重命名: `DAY1-agent 架构.mp4` → `DAY1-agent-architecture.mp4`
- ✅ 移除了文件名中的空格和中文字符
- ✅ 文件大小: 83MB (正常)
- ✅ 更新了所有引用路径

### 2. 时长修正
- ✅ `course-data.js`: duration 从 "8:00" 改为 "2:00"
- ✅ `open-course.html`: 显示从 8:00 改为 2:00

### 3. 数据完整性
- ✅ 添加了 Day 2-7 的占位数据
- ✅ Day 1 有完整的 3 个章节数据
- ✅ 每个章节都有 content, corePoints, codeExample, bestPractices

## 调试步骤

### 步骤 1: 测试数据加载

打开: `debug.html`
```bash
open "/Users/anker/Desktop/CC project/agent/debug.html"
```

**期望结果**:
- ✓ courseData 已加载
- 数据项数量: 7
- 视频应该能播放

### 步骤 2: 测试内容渲染

打开: `simple-test.html`
```bash
open "/Users/anker/Desktop/CC project/agent/simple-test.html"
```

**期望结果**:
- 能看到 Day 1 的完整数据
- 能看到 3 个章节的内容（什么是Agent、ReAct模式、工具调用）

### 步骤 3: 独立系统测试

打开: `standalone-test.html`
```bash
open "/Users/anker/Desktop/CC project/agent/standalone-test.html"
```

**期望结果**:
- 视频能加载
- 章节内容完整显示
- 包含表格、列表、代码示例

### 步骤 4: 浏览器控制台检查

打开 `course.html` 后：
1. 按 F12 或 Cmd+Option+I 打开开发者工具
2. 切换到 Console 标签页
3. 查看是否有红色错误信息

## 可能的问题原因

### 问题 1: JavaScript 未执行

**症状**: 页面显示空白，但HTML结构正常

**检查**:
```javascript
// 在浏览器控制台输入:
console.log(typeof courseData);
// 应该输出: "object"

console.log(courseData[0].chapters.length);
// 应该输出: 3
```

**解决**:
- 确保 `course-data.js` 和 `course-app.js` 都成功加载
- 检查浏览器控制台是否有语法错误

### 问题 2: 章节渲染失败

**症状**: 导航栏有内容，但中间主内容区空白

**检查**:
```javascript
// 在浏览器控制台输入:
document.getElementById('chapters-container').innerHTML;
// 应该有大量HTML内容
```

**解决**:
- 检查 `renderChapters()` 函数是否被调用
- 检查 template literals 中的HTML是否有语法错误

### 问题 3: 视频路径问题

**症状**: 视频显示 "视频制作中..."

**检查**:
```javascript
// 在浏览器控制台输入:
document.getElementById('main-video').src;
// 应该显示完整路径
```

**解决**:
- 确认视频文件在正确位置
- 尝试在浏览器中直接访问视频URL

## 文件清单

### 主要文件
- ✅ `course.html` - 主课程页面
- ✅ `course-data.js` - 课程数据 (418行)
- ✅ `course-app.js` - 应用逻辑 (388行)
- ✅ `open-course.html` - 启动页面

### 测试文件
- 🔧 `debug.html` - 数据加载测试
- 🔧 `simple-test.html` - 内容渲染测试
- 🔧 `standalone-test.html` - 独立系统测试

### 视频文件
- ✅ `../lessons/DAY1-agent-architecture.mp4` (83MB, 2分钟)

## 下一步

1. 打开 `standalone-test.html` 查看是否能正常显示内容
2. 如果 standalone 版本正常，说明问题在 `course-app.js` 的逻辑
3. 如果 standalone 也空白，说明问题在 `course-data.js`
4. 打开浏览器控制台查看具体错误信息
5. 提供控制台截图以便进一步诊断

## 常见解决方案

### 清除缓存
```bash
# Chrome/Safari: Cmd + Shift + R (硬刷新)
# 或者在开发者工具中右键刷新按钮 → "清空缓存并硬性重新加载"
```

### 检查文件权限
```bash
ls -la "/Users/anker/Desktop/CC project/agent/"
# 所有.html和.js文件应该有读取权限
```

### 重新加载页面
```bash
# 关闭所有相关标签页
# 重新打开 course.html
open "/Users/anker/Desktop/CC project/agent/course.html"
```
